// 'use strict';


outer: for (let i = 0; i < 3; i++) {
    for (let j= 0; i < 3; j++) {
        let input = prompt(`Coord. Value: ${i};${j}`, "");
    }   if (!input) break outer;
        // Действие
    break
}
